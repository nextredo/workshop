# Enums
- Spicy variant / type-safe union type baked into the language
- `Option` enum
  - A value that's something or nothing
- Encodes meaning w/ data
- Pattern matching
  - For running different code based on the enum
  - `match`
  - `if let`
